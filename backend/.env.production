# Production Environment Configuration for Audion Backend
# This file documents all required environment variables for production deployment
# Never commit actual secret values - use Render Dashboard or secure vaults

# === CRITICAL: Generate a strong JWT secret before deployment ===
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=<your-generated-secret-key-here>

# === Environment Mode ===
ENVIRONMENT=production

# === MongoDB Atlas Configuration ===
# Connection string format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
MONGO_URL=mongodb+srv://user:password@cluster.mongodb.net/audion_atlas_DB?retryWrites=true&w=majority
DB_NAME=audion_atlas_DB

# === OpenAI API Configuration ===
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxx

# === AWS S3 Configuration ===
# For audio file storage (required for production - Render has ephemeral filesystem)
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_REGION=ap-southeast-2
S3_BUCKET_NAME=audion-audio-files

# === Backend URL Configuration ===
# Used to generate URLs for audio files in responses
# Should point to the production backend URL
BACKEND_URL=https://audion.onrender.com
MOCK_AUDIO_URL_BASE=https://audion.onrender.com

# === Logging Configuration ===
LOG_LEVEL=INFO

# === Python Version (for Render) ===
PYTHON_VERSION=3.13

# === Notes ===
# 1. Never commit this file with actual secrets
# 2. Use Render Dashboard to set all environment variables
# 3. Regenerate JWT_SECRET_KEY when rotating credentials
# 4. Ensure MongoDB Atlas allows connections from Render IP range
# 5. S3 bucket must have public read access for audio playback
# 6. Render automatically assigns PORT via $PORT environment variable
