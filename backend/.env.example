# ==============================================================================
#  Audion Backend Environment Variables (.env.example)
# ==============================================================================
# このファイルは環境変数のテンプレートです。
# 実際の開発を始める前に`.env`という名前でコピーし、
# 各自の環境に合わせて値を設定してください。
# `.env`ファイルは`.gitignore`に含まれているため、Gitリポジトリにはコミットされません。

# ------------------------------------------------------------------------------
# アプリケーション基本設定 (General Application Settings)
# ------------------------------------------------------------------------------
# ENVIRONMENT: アプリケーションの実行環境 (development, staging, production)
# production環境ではDEBUGをFalseに設定してください。
ENVIRONMENT=development
DEBUG=True

# SECRET_KEY: アプリケーションのセキュリティキー。JWTの署名など、暗号化を必要とする多くの機能で使用されます。
# 以下のコマンドなどで生成した、長くてランダムな文字列を設定してください。
# openssl rand -hex 32
SECRET_KEY=your_super_secret_random_string_here

# ------------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing) 設定
# ------------------------------------------------------------------------------
# フロントエンドのアクセスを許可するオリジンのリスト（カンマ区切り）。
# 開発環境ではExpoのデフォルトポートなどを設定します。
# 本番環境では実際のフロントエンドのドメインを設定してください。
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006,http://127.0.0.1:8081

# ------------------------------------------------------------------------------
# データベース設定 (MongoDB)
# ------------------------------------------------------------------------------
# MongoDBの接続URI（server.py の実装に合わせた変数名）
MONGO_URL=mongodb://localhost:27017
DB_NAME=audion_dev

# ------------------------------------------------------------------------------
# 認証設定 (JWT - JSON Web Token)
# ------------------------------------------------------------------------------
# JWTの署名アルゴリズム。HS256が一般的です。
JWT_ALGORITHM=HS256
# アクセストークンの有効期限（分）
ACCESS_TOKEN_EXPIRE_MINUTES=30
# リフレッシュトークンの有効期限（日）
REFRESH_TOKEN_EXPIRE_DAYS=7

# ------------------------------------------------------------------------------
# AIサービス設定 (OpenAI)
# ------------------------------------------------------------------------------
# OpenAI APIのAPIキー
OPENAI_API_KEY=sk-your_openai_api_key_here
# 使用するGPTモデル名
OPENAI_MODEL_NAME=gpt-4-turbo

# ------------------------------------------------------------------------------
# 音声合成サービス設定 (Google Text-to-Speech)
# ------------------------------------------------------------------------------
# Google Cloudのサービスアカウントキー(JSON)へのパス。
# この環境変数を設定すると、Google Cloudライブラリが自動で認証情報を読み込みます。
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/google-cloud-service-account.json

# ------------------------------------------------------------------------------
# ファイルストレージ設定 (AWS S3)
# ------------------------------------------------------------------------------
# プロフィール画像などのファイルアップロードに使用するS3バケットの情報。
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=ap-northeast-1
S3_BUCKET_NAME=your-s3-bucket-name-for-profile-images
