# Audion Backend - Environment Variables Template
# Copy this file to .env and fill in with actual values
# REQUIRED variables MUST be set - no defaults available

# ============================================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================================
MONGO_URL=mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority
DB_NAME=audion_dev_db

# ============================================================
# SECURITY & AUTHENTICATION (REQUIRED - NO DEFAULTS)
# ============================================================
# Generate a strong JWT secret with:
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-strong-random-key-here-at-least-32-characters

# ============================================================
# API KEYS (REQUIRED - NO DEFAULTS)
# ============================================================
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...your-openai-api-key...

# ============================================================
# AWS S3 CONFIGURATION (REQUIRED - NO DEFAULTS)
# ============================================================
# Get credentials from: https://console.aws.amazon.com/iam/home
AWS_ACCESS_KEY_ID=AKIA...your-aws-access-key...
AWS_SECRET_ACCESS_KEY=...your-aws-secret-key...
AWS_REGION=ap-southeast-2
S3_BUCKET_NAME=audion-audio-files

# ============================================================
# NETWORK & SERVER CONFIGURATION (OPTIONAL - HAS DEFAULTS)
# ============================================================
# CORS allowed origins (comma-separated)
# Default: https://audion.onrender.com,http://localhost:3000,http://localhost:5173,exp://localhost:19000,exp://localhost:19001
CORS_ALLOWED_ORIGINS=https://audion.onrender.com,http://localhost:3000,http://localhost:5173,exp://localhost:19000,exp://localhost:19001

# File server URL for serving images and static files
# Development: http://localhost:8001
# Production: https://audion.onrender.com
FILE_SERVER_URL=http://localhost:8001

# Mock audio URL base for test/fallback content
# Default: http://localhost:8001
MOCK_AUDIO_URL_BASE=http://localhost:8001

# ============================================================
# DATABASE TIMEOUTS (OPTIONAL - HAS DEFAULTS)
# ============================================================
# Timeout for database ping operations (seconds)
# Default: 5.0
DB_PING_TIMEOUT=5.0

# Timeout for database operations (seconds)
# Default: 10.0
DB_OPERATION_TIMEOUT=10.0

# Timeout for batch database operations (seconds)
# Default: 5.0
DB_BATCH_TIMEOUT=5.0

# ============================================================
# CACHING (OPTIONAL - HAS DEFAULTS)
# ============================================================
# RSS feed cache expiry time (seconds)
# Development: 300 (5 minutes for testing)
# Production: 1800 (30 minutes to reduce API calls)
# Default: 300
RSS_CACHE_EXPIRY_SECONDS=300

# ============================================================
# CONTENT GENERATION (OPTIONAL - HAS DEFAULTS)
# ============================================================
# Recommended word count for article summaries
# Default: 250
RECOMMENDED_WORD_COUNT=250

# Recommended word count for insights/analysis
# Default: 350
INSIGHT_WORD_COUNT=350

# ============================================================
# OPENAI MODEL CONFIGURATION (OPTIONAL - HAS DEFAULTS)
# ============================================================
# Chat model for text generation
# Options: gpt-4o, gpt-4-turbo, gpt-4, gpt-3.5-turbo
# Default: gpt-4o
OPENAI_CHAT_MODEL=gpt-4o

# Text-to-speech model
# Options: tts-1 (low latency), tts-1-hd (higher quality)
# Default: tts-1
OPENAI_TTS_MODEL=tts-1

# Text-to-speech voice
# Options: alloy, echo, fable, onyx, nova, shimmer
# Default: alloy
OPENAI_TTS_VOICE=alloy

# Test model for testing/fallback
# Default: gpt-3.5-turbo
OPENAI_TEST_MODEL=gpt-3.5-turbo

# ============================================================
# APPLICATION ENVIRONMENT
# ============================================================
# Environment type: development, production, staging
ENVIRONMENT=development

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================
# NOTES
# ============================================================
# 1. REQUIRED variables (marked above) MUST be set
#    - Missing required variables will cause startup failure
#    - No default fallback values are provided for security
#
# 2. OPTIONAL variables have sensible defaults
#    - Can be omitted unless you need custom values
#    - Development defaults are included
#
# 3. Production vs Development
#    - Use different values for DB_NAME (audion_prod_db vs audion_dev_db)
#    - Set higher RSS_CACHE_EXPIRY_SECONDS in production (1800)
#    - Use production FILE_SERVER_URL (https://audion.onrender.com)
#
# 4. Security
#    - Never commit real credentials to git
#    - Use .gitignore to exclude .env files
#    - Rotate AWS credentials regularly
#    - Keep JWT_SECRET_KEY secure and unique
